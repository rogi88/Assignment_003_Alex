import { TextDecorationDirective } from './text-decoration.directive';

describe('TextDecorationDirective', () => {
  it('should create an instance', () => {
    const directive = new TextDecorationDirective();
    expect(directive).toBeTruthy();
  });
});
